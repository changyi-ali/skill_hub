# AGENTS.md

本仓库只管理用户自创或明确接管维护的个人 Codex Skills。

## 运行目录与存档目录

- 运行副本：`/Users/richard/.codex/skills/<skill-name>`
- Git 存档：`skills/<skill-name>`

运行副本与存档必须保持逐文件一致，但不要用软链接替代任一目录。

## 新建或修改个人 Skill

必须使用 `managing-personal-skills`，并完成以下闭环：

1. 在运行目录创建或修改 Skill；
2. 使用官方 `quick_validate.py` 验证；
3. 完整同步到本仓库的同名目录；
4. 用递归比较验证两个目录一致；
5. 检查敏感信息、未完成占位符和 Git diff；
6. 只暂存本次相关文件，提交并推送上游远端；
7. 推送后验证本地提交与远端跟踪分支一致。

## 排除范围

不得把系统 Skill、插件 Skill、缓存文件或带外部来源标记的第三方 Skill 复制进来。发现来源不明确时先询问用户，不要猜测所有权。

不得覆盖或删除无关改动。仓库不干净时只提交本任务拥有的精确路径；如果目标路径存在无法区分来源的改动，停止并询问用户。
